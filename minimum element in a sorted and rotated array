class Solution{
public:
    int findMin(int arr[], int n){
        int low=arr[0];
        for(int i=0;i<n;i++)
        {
            if(arr[i]<low)
            {
                low=arr[i];
            }
        }
        return low;
    }
};
