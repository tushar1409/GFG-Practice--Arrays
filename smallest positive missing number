class Solution
{
    public:
    //Function to find the smallest positive number missing from the array.
    int missingNumber(int arr[], int n) 
    { 
        int s=1;
        sort(arr,arr+n);
        for(int i=0;i<n;i++)
        {
        
                if(arr[i]==s)
                {
                    s++;
                }
            }
            return s;
        }
        
    
};
